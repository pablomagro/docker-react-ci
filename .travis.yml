sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t pmagas/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pmagas/docker-react npm run test
